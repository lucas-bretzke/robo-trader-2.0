{
  "name": "robo-trader-backend",
  "version": "1.0.0",
  "description": "Backend para o Robo Trader 2.0",
  "main": "index.js",
  "scripts": {
    "start": "python app.py",
    "dev": "python app.py",
    "setup": "pip install setuptools wheel && pip install -r requirements.txt",
    "setup:env": "python -m venv venv && .\\venv\\Scripts\\activate && npm run setup",
    "start:full": "npm run setup && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "python": ">=3.8.0 <3.12.0"
  }
}
